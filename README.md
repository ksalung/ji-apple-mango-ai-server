# apple-mango-ai-server!
apple mango ai server

## (25.12.08 - 1) pip install 라이브러리 모음 (구동 시 설치 필요)
supabase connect
`pip install python-dotenv psycopg2`

psycopg2 설치 실패 시 `pip install psycopg2-binary`

## (25.12.08 - 2) .env 설정 수정
SQL_USER={id}
SQL_PASSWORD={pw}
SQL_HOST={host}
SQL_PORT=6543
SQL_DATABASE=postgres
